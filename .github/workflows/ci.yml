name: hello-world
on: push
jobs:
  ci-job:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
        node-version: 22
    - run: npm install
    - run: node runwf.js .github\workflows\helloworld.yml
            